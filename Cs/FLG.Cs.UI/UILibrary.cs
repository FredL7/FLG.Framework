using FLG.Cs.Math;
using FLG.Cs.UI.Grid;

namespace FLG.Cs.UI {
    public static class UILibrary {
        /* ============================= *
         * ========== layouts ========== *
         * ============================= */

        /*public static void HStack( // TODO: Add default
            string name,
            Spacing margin, Spacing padding,
            float width, float height,
            int order, bool isTarget,
            EGridDirection direction,
            EGridJustify justify,
            EGridAlignment alignment)
        {
            HStack hstack = new(name, width, height, margin, padding, order, isTarget, direction, justify, alignment);
            // TODO: Add result to pipeline and notify Front-end UI
            // Also change it so ctor goes through here with XMLNode instead of directly using new?
        }

        public static void VStack(
            string name,
            Spacing margin, Spacing padding,
            float width, float height,
            int order, bool isTarget,
            EGridDirection direction,
            EGridJustify justify,
            EGridAlignment alignment)
        {
            VStack vstack = new(name, width, height, margin, padding, order, isTarget, direction, justify, alignment);
            // TODO: Add result to pipeline and notify Front-end UI
        }*/

        /* ============================= *
         * ========== Widgets ========== *
         * ============================= */

        public static void Button(string name)
        {

        }

        // TODO: Others
    }
}
